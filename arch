mixnet

COMMANDS

mixnet setup -name "NAME"
 - creates pub/priv RSA key
 - saves privkey to /etc/mixnet/privkey.pem
 - submits {name: pubkey} to gcloud endpoint.
 - gets dict of {ip_addr: pubkey} from gcloud, saves to /etc/mixnet/hosts

mixnet start
  - opens socket on port PORT
  - daemonizes mixnet

LOGIC

on fwd packet:

on local packet:
 - create mixchain from /etc/mixnet/hosts
 - send mixchain to ip


DATA TYPES
